<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ProyectoEliminarActivity" >

<EditText
        android:id="@+id/txtProyectoEliminar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:ems="10"
        android:hint="@string/idProyecto"
        android:maxLength="50"
        android:inputType="number" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/btnEliminarProyecto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/txtProyectoEliminar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:onClick="eliminarProyecto"
        android:text="@string/eliminar" 
        android:background="@drawable/btn_eliminar"
       	android:padding="5dp"
        android:textColor="@android:color/white"
        />
</RelativeLayout>
